/**
 * UI spécifique onglet Repas — pas de logique CRUD ici
 * (NutriSmartCRUD + nutrismart-crud-admin.js).
 */
(function () {
  "use strict";

  document.addEventListener("DOMContentLoaded", function () {
    if (!document.getElementById("panel-repas")) return;
  });
})();
